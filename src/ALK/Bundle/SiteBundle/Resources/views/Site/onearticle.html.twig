<h2>
    <a href="{{ path('ALKSiteBundle_voirarticle', {'id': article.id}) }}" title="Permalink" >#</a>
    {{ article.titre }}
    <span class="pull-right">
        <small>{{ article.date|date('d/m/Y') }}</small>
    </span>
</h2>
<p>
    <small>
        {% for tag in article.tags %}
            <a href="{{ path('ALKSiteBundle_voirlesArticles', {'tag': tag.name}) }}" ><i class="icon-tag"></i> {{ tag.name|lower }}</a>
        {% endfor %}
    </small>
</p>

<p class="mesarticles" >{{ article.contenu|raw }}</p>